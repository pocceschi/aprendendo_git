# items() -- retorna em forma de listas os elementos do dicionário
# faz uso de tuplas, constando uma chave e um dicionário

# values() -- retorna em forma de lista somente os dados,
# descartando as chaves

# keys() -- retorna em forma de lista somente as chaves,
# descartando os dados

# has_key() -- método que retorna se existe ou não uma chave
# dentro de um dicionário. Irá retornar 1 para True e 0 para False

# clear() -- esvazia o dicionário completamente

# pop(chave) -- irá deletar a chave informada dentro do parêntese

# popitem() -- deleta a ultima chave do dicionário